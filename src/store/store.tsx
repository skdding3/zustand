// import { create } from "zustand";
// import { devtools } from "zustand/middleware";

// type BearStore = {
//   bears: number;
//   increaseBear: () => void;
//   removeAllBears: () => void;
// };

// const bearStore = (set) => ({
//   bears: 0,
//   increaseBear: () => set((state) => ({ bears: state.bears + 1 })),
//   removeAllBears: () => set({ bears: 0 }),
// });

// export const useBearStore = create(devtools(bearStore));
